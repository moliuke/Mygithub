V1.1.3
1、增加了update.c文件，将原来在Uart_updateTask.c中的涉及到定时器注册的部分移到该文件，
	并且重新封装了开关屏接口、设置箱体大小、屏体大小这些参数，其中开关屏接口增加了写
	配置文件记录屏体状态
2、配置文件增加了LED屏的状态是开屏的还是关屏的
3、设备启动后，原来的前3分钟每隔3秒钟就发一次开屏指令，现在是在读取到配置文件中的LED屏状态
   如果记录的是关屏状态，则启动后不开屏，否则按照原来的前3分钟每3秒钟发一次开屏指令
4、通信协议以及协议下的不通的客户都有一个init.c文件，里面是初始化本协议或者客户特殊需求的
   接口的初始化
5、是否开启像素检测功能所有协议默认不开启，如果某一协议的某一客户有需求则在该客户的init.c文件
	对该检测接口重新初始化即可。每个协议的默认的播放列表、默认显示信息同理，这些接口在PTC_init.c
	中定义。
6、增加了1位、4位、8位、16位、32位深度bmp图片的支持，并矫正了图片越过屏屏幕边界时显示呈倒置的现象
7、修复了因配置文件屏体大小箱体大小配置不对而导致像素数据量大于用于存储像素数据的缓存时程序挂掉的bug

V1.2.0
1、可以直接只保留一个协议，其他的删掉也可以编译，并且seewor协议下只保留一个特定项目，其他的删掉也可以编译通过
2、增加了一个6168的端口号，专用于适应显科工具的收发文件，其他任何协议都可以通过该端口收发文件

V1.2.1
1 白天6点--19点期间两个光敏值都为0时,默认设置亮度为20

V1.2.2
1、修复了图片不存在时程序继续往下走的bug，即在IMG_decoder函数里面对容错返回-1

V1.2.3
1、马来西亚项目自动居中
2、增加对ASC12、ASC14的支持
3、增加像素故障率超过50%时自动关屏的功能
4、增加对TXRX卡重启的功能
5、增加复位接收卡发送卡的功能


